#include <stdio.h>
#include <ctype.h>
#include <string.h>

/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
*/
/* Program that prints the alphabet in lowercase */
int main(void)
{
	char str_lower_case = 'S';

	putchar(tolower(str_lower_case));
	putchar('\n');
	return (0);
}
